# Rentle — Progress

Living doc. Updated at the end of every phase. Full plan: `~/.claude/plans/hey-hey-how-are-fuzzy-bee.md`.

## HIBERNATED as of 2026-08-20 — read this first

Project is paused indefinitely, not abandoned. Everything below this line is
historical and still accurate as of the pause. All local build artifacts,
caches, and `node_modules` were deleted to save disk space — they are fully
regenerable and hold no project state. Nothing else was touched or deleted.

**To resume:**
1. `cd server && npm install` (rebuilds `node_modules`, `dist/`)
2. `cd mobile && flutter pub get` (rebuilds `.dart_tool/`, `build/`, plugin registrant files, Gradle cache)
3. Confirm `server/.env` still has the right values (it was left in place, untouched, git-ignored) — DB/Clerk/Supabase creds don't rotate on their own, but double-check nothing expired
4. Read the last "Mobile:" and "Server:" sections below for exactly where feature work stopped

**Where it actually stopped (last commit `c1551b2`, mobile UI revamp round 2):**
- Server (Phases 0–4): complete, deployed on Render (`https://rentle-gdys.onrender.com`), CI green. No open work.
- Mobile: full owner+tenant loop, staff management, notices, tenant discovery all built. A second visual-design pass (shared widgets, Plus Jakarta Sans, large titles, a real bottom-sheet duplicate-save fix) is committed but **not yet verified on a real device or through a real authenticated flow** — it only got as far as an iOS Simulator check via a throwaway harness, because the Clerk `pk_test_...` publishable key was never supplied. That's the actual next step whenever this picks back up: get the key, run it for real, then decide what's next (Phase 6 web dashboard, or the deferred CRM/booking/accounting/live-payments scope in plan §5).
- Nothing is uncommitted. `git log` on `main` is the full source of truth for what's built.

---

## Phase 0 — Foundations: COMPLETE

**Goal:** repo scaffold, Supabase + Clerk projects, Prisma schema v1, Fastify auth pipeline, RLS proven, cross-org isolation test suite in CI, `server/` deployed to Render. **All exit criteria met — see plan §4.**

### Done
- Git repo pushed to `github.com/Amaans04/Rentle` (private).
- `server/` Fastify + TypeScript project — builds clean, typechecks clean, 0 npm vulnerabilities.
- `server/prisma/schema.prisma` — 22 models: full core-property-ops schema plus `Subscription`/`OrgFeatureFlag` for tier gating.
- **Supabase project live and migrated.** `app_user` role created (least-privilege, `NOSUPERUSER NOBYPASSRLS`), initial migration applied (all 22 tables exist), `prisma/rls.sql` applied for real.
- **Cross-org isolation proven against the live database — in CI, not just locally.** `RUN_DB_TESTS=true` is set as a GitHub Actions secret; every CI run now executes all 9 tests (0 skipped), including 3 that actually insert/query across two real orgs and confirm RLS blocks cross-org reads (ORM path and raw-SQL bypass attempt both blocked; same-org access still works).
- Auth pipeline (`src/auth/`): `authenticate`, `resolveOrg`, `requirePermission`, `requireFeature` (correctly wrapped in `withOrgContext`), `withOrgContext`.
- Clerk webhook (`src/routes/webhooks/clerk.ts`) — sole provisioning path for `User`/`Organization`/`OrganizationMember`. Live at `https://rentle-gdys.onrender.com/webhooks/clerk`, signing secret configured.
- `/me`, `/internal/health` (secret-protected, real DB round-trip), `/health` (public liveness check for Render) routes — all verified live in production.
- `.github/workflows/ci.yml` (runs on every push, `workflow_dispatch` for manual runs), `.github/workflows/daily-jobs.yml`, `render.yaml`.
- **`server/` deployed and live on Render**: `https://rentle-gdys.onrender.com`. Verified: public health check 200s, secret-protected route correctly rejects/accepts, unauthenticated `/me` correctly 401s, Render→Supabase connectivity confirmed in production.
- All 10 GitHub Actions repo secrets set (`DATABASE_URL`, `DIRECT_URL`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `CLERK_SECRET_KEY`, `CLERK_PUBLISHABLE_KEY`, `CLERK_WEBHOOK_SECRET`, `INTERNAL_CRON_SECRET`, `SERVER_URL`, `RUN_DB_TESTS`).

### Real gotchas hit and fixed while wiring Supabase (worth knowing if this ever needs redoing)
1. **Superuser bypasses RLS entirely** — the whole reason `app_user` exists instead of using the default `postgres` connection. Documented at the top of `rls.sql`.
2. **Supabase's pooler needs `role.project-ref` as the username** for the pooled connection (port 6543) — plain `app_user` fails; needs `app_user.<project-ref>`.
3. **Direct connections (port 5432) require IPv6** — Supabase's direct host has no A record on newer projects. Migrations use `directUrl` for this reason; if IPv6 isn't available, this can fail — use the pooler for everything if so.
4. **Prisma CLI (`db execute`) hung indefinitely** against this specific pooler in this environment for unclear reasons, even though raw TCP/TLS/Postgres-protocol tests (verified by hand with raw sockets and a real `pg` client) all worked instantly. `prisma migrate deploy`/`resolve` worked fine via `directUrl`; only ad hoc `db execute` calls hung. If this recurs, fall back to a raw `pg` client script rather than fighting the CLI.
5. **Migrations need more than `USAGE`/`SELECT,INSERT,UPDATE,DELETE`** — also needed `GRANT CREATE ON SCHEMA public` and, non-obviously, `GRANT CREATE ON DATABASE postgres` (required just for `CREATE SCHEMA IF NOT EXISTS "public"`, even though `public` already existed).
6. **Supabase installs extensions (`pgcrypto`, `citext`) into a schema literally called `extensions`, not `public`.** Prisma's connector pins `search_path` to `public` on every connection regardless of the role's own default search_path, so schema-qualifying in `schema.prisma` (`citext(schema: "extensions")`) was necessary — and even then, Prisma only schema-qualifies the `CREATE EXTENSION` statement, not column type references (`CITEXT` in a `CREATE TABLE`), which had to be hand-patched to `"extensions"."citext"` in the generated migration SQL. Also needed `GRANT USAGE ON SCHEMA extensions TO app_user`.
7. **`organizations` and `organization_members` are deliberately NOT RLS-protected**, unlike every other table — full reasoning is in the comment block at the top of `rls.sql`. Short version: both have a genuine chicken-and-egg problem (org creation happens before any org context can exist; `resolveOrg()` queries membership *to establish* context, so it can't require that context as a precondition). The isolation guarantee for these two comes from application-level checks (signature-verified webhook as sole writer; explicit `WHERE organizationId = ? AND userId = ?` in `resolveOrg`), not RLS. Every table holding actual tenant data keeps full RLS.
8. **Any Prisma query against an RLS-protected table must go through `withOrgContext`**, even ones that already have an explicit `WHERE organizationId = ...` filter in the query itself — RLS checks the session variable, not the query's own WHERE clause, so an unscoped connection returns zero rows regardless of what the query asks for. Caught and fixed one instance of this in `requireFeature` (was doing a plain `prisma.orgFeatureFlag.findUnique` instead of wrapping it). **This is the pattern every future Phase 1+ route handler must follow** — a good thing to double check in review.

### One thing to double check
- Confirm `CLERK_WEBHOOK_SECRET` is also set in **Render's** Environment tab (it was added to `.env` and GitHub secrets, but Render was originally deployed before the webhook existed — make sure that value made it there too and the service redeployed after).

## Phase 1 — Property hierarchy + staff/RBAC: COMPLETE

**Goal:** Org/Property/Building/Floor/Room/Bed CRUD + bed-status-machine endpoint, staff invite + role assignment, audit-logging wired into every mutation. **All exit criteria met — see plan §4.**

### Done
- Shared route infra: `src/lib/http-errors.ts` (typed `HttpError`/`NotFoundError`/`ValidationError`/`ConflictError`, checked via `instanceof` in the global error handler), `src/lib/validation.ts` (`parseOrThrow` for zod schemas).
- `src/routes/organizations.ts` — GET/PATCH org, member list/update/deactivate.
- `src/routes/properties.ts` — full CRUD, with an explicit fix: `requirePermission`'s property-scope check only fires when `:propertyId` is in the URL, which POST (create) never has — so a property-scoped manager could otherwise create unrestricted new properties. Fixed with an explicit `member.propertyIds.length > 0` check in the handler.
- `src/routes/buildings-floors.ts`, `src/routes/rooms-beds.ts` — CRUD, rooms/beds flattened by `propertyId` with optional filters per plan §3 (not deep URL nesting).
- `src/services/bed-status.ts` + `PATCH /beds/:bedId/status` — the bed-status-machine endpoint. `OCCUPIED` is unreachable in either direction through this endpoint (only a Tenancy, Phase 2, can set it) — enforced in code now, tested, ready for Phase 2 to hook in.
- `src/routes/staff.ts` — invite endpoint calls Clerk's `createOrganizationInvitation` (coarse `org:member` Clerk role only — granular business role/propertyIds recorded in the audit log now, applied via `PATCH /members/:memberId` once they've joined, not pre-set through Clerk metadata); `StaffProfile` upsert/get for salary/joinDate.
- Every mutating route wraps its write in `withAudit` — verified by direct count, not just spot-checked (mutation-route count == `withAudit`-call count in every route file).
- **New test file** `tests/phase1-property-hierarchy.test.ts` — 8 tests against the live database (only Clerk's token *verification* is mocked, trusting Clerk's own well-tested implementation; everything downstream — RLS, authorization, business logic — runs for real). Covers: full property→building→floor→room→bed creation via API calls only; a property-scoped manager blocked from creating new properties, reading/writing outside their `propertyIds`, with the property list itself correctly filtered; the bed-status-machine rejecting manual `OCCUPIED` and requiring `blockedReason` for `BLOCKED`; every mutation producing an audit row. All 17 tests (9 from Phase 0 + 8 new) pass together; zero orphaned test data after cleanup.

## Phase 2 — Tenancy lifecycle: COMPLETE

**Goal:** Tenancy CRUD + invite/onboarding-token flow, document upload via signed URLs, transfer/notice/move-out endpoints, `TenancyEvent` timeline, tenant self-auth via Clerk. **All exit criteria met — see plan §4.**

### Done
- **Supabase Storage bucket (`tenant-documents`) created programmatically** via the API using the existing service-role key — no manual dashboard step needed. Signed upload/download URLs verified working end-to-end.
- **A real schema gap surfaced and solved without a migration**: `Tenancy.userId` is required, so a tenant can't have a Tenancy row until they've actually signed up via Clerk — but the owner needs to send an invite *before* that. Solved with a **signed, stateless onboarding token** (`src/lib/onboarding-token.ts`, HMAC-SHA256 derived from the existing `CLERK_SECRET_KEY` — no new secret needed) carrying the invite's details (org/property/bed/rent/deposit + expiry). No dedicated invite table; the bed goes `RESERVED` immediately, and the real `Tenancy` row is only created when the tenant actually accepts.
- **Tenants aren't OrganizationMembers**, so they hit the same "no org context yet" problem staff did — but worse, since even *looking up their own tenancy* needs org context first. Solved with a parallel resolver, `resolveTenantContext` (`src/auth/pipeline.ts`), mirroring `resolveOrg` but keyed on `(userId, organizationId) → their own non-archived Tenancy`, never a client-supplied tenancy ID. The one endpoint that runs *before* any Tenancy exists (`POST /tenant/onboarding/accept`) is the deliberate exception — trust comes from the signed token's HMAC, the same role membership-lookup plays elsewhere.
- `src/services/tenancy-status.ts` — the tenancy state machine (`PENDING_ONBOARDING → ACTIVE → NOTICE_GIVEN → ARCHIVED`, plus direct `ACTIVE/PENDING_ONBOARDING → ARCHIVED`), mirroring Phase 1's bed-status-machine pattern.
- `src/routes/tenancies.ts` (org-scoped: invite, CRUD, activate/notice/move-out/transfer, staff-side documents) and `src/routes/tenant.ts` (tenant-self: onboarding accept, `/tenant/me`, documents) — `Bed.status` now genuinely moves `RESERVED → OCCUPIED → VACANT` in step with the tenancy lifecycle, via direct writes inside the tenancy services (the Phase 1 manual status endpoint's restrictions are specifically about staff fudging status by hand, not about this).
- **Closed a real CI gap while here**: `tsconfig.json` excluded `tests/`, so `npm run typecheck` (and CI) had never actually type-checked a single test file — only vitest's transpile-only transform touched them. Added `tsconfig.test.json` + wired it into the `typecheck` script.
- Extracted a shared `tests/sweep-stale-test-data.ts` (FK-safe deletion order) used by both Phase 1 and Phase 2 test files, replacing each file's own partial inline version.
- **New test file** `tests/phase2-tenancy-lifecycle.test.ts` — 15 tests against live Supabase. Full invite → accept → activate → transfer → notice → move-out cycle via real API calls, `Bed.status` checked at every step, invalid transitions rejected by the status machine, cross-org tenant isolation, and document upload+confirm. All 32 tests (17 from Phases 0–1 + 15 new) pass together; zero orphaned test data after cleanup.

## Phase 3 — Invoicing + manual payments: COMPLETE

**Goal:** Invoice/line-item generation (manual trigger, then wired to the daily GitHub Actions cron), manual payment recording, `PaymentGatewayProvider` interface with only a manual binding. **All exit criteria met — see plan §4.**

### Done
- New `idempotency_keys` table (migrated + RLS-protected, same pattern as every other org-scoped table) backs the `Idempotency-Key` header — no Redis needed at this scale. `src/lib/idempotency.ts` wraps just the business-logic part of a handler, same calling convention as `withAudit`/`withRequestOrgContext`. Known, accepted limitation at pilot scale: it's a check-then-act pattern, not a lock, so two genuinely concurrent requests with the same key could both slip through — fine for a handful of pilot users, would need real locking before it matters at scale.
- `src/services/invoicing.ts` — due-date math (`rentDueDay` clamped to the actual last day of the month), grace-period math (`OVERDUE` vs `SENT` decided at generation time), and `generateInvoicesForProperty`, shared by both the manual per-property trigger and the daily cron so there's exactly one generation code path, not two to keep in sync.
- Generation is **idempotent at the domain level**, independent of the `Idempotency-Key` header: one invoice per `(tenancy, period)`, so running it twice (or once a day, forever, via cron) for a period that's already invoiced is a safe no-op — this is what makes the daily cron safe to fire regardless of each property's own `rentDueDay`.
- `src/routes/payments.ts` — manual entry only (`CASH`/`UPI`/`BANK_TRANSFER`/`CHEQUE`/`WALLET`; `RAZORPAY`/`CASHFREE` rejected by validation, schema-ready for later). Updates `Invoice.paidAmount`/`status` (`PARTIALLY_PAID`/`PAID`) correctly, rejects payments exceeding the remaining balance, and calls the existing `PaymentGatewayProvider` interface (manual binding) rather than writing gateway-shaped code directly into the route.
- `src/routes/internal/invoices.ts` — the cron endpoint. `organizations` has no RLS (per the Phase 0 decision), which is exactly the correct, intended use of that exception here: a trusted machine-to-machine job legitimately iterating every org, not a per-request handler trusting client input.
- `.github/workflows/daily-jobs.yml` — the invoice-generation step (previously commented out, inert) is now live.
- **New test file** `tests/phase3-invoicing-payments.test.ts` — 10 tests against live Supabase. Due-date/period math verified against the same pure functions the service uses (not hand-recomputed in the test, so a bug in the math would have to be wrong the same way twice to slip through), grace-period branch exercised with a genuinely past period, partial→full payment status transitions, overpayment and live-gateway-method rejection, an `Idempotency-Key` proven to prevent a real duplicate `Payment` row (not just a duplicate response), and the cron endpoint's auth + cross-org generation. All 42 tests (32 from Phases 0–2 + 10 new) pass together; zero orphaned test data after cleanup.

## Phase 4 — Complaints & notices: COMPLETE

**Goal:** Complaint/comment CRUD + assignment/status transitions; Notice CRUD + audience targeting (in-app only). **All exit criteria met — see plan §4.**

### Done
- `src/services/complaint-status.ts` — complaint state machine (`OPEN → IN_PROGRESS/ESCALATED/CLOSED → RESOLVED → CLOSED/REOPENED`), same pattern as the bed/tenancy status machines.
- `src/services/notice-visibility.ts` — the actual audience-targeting logic as one testable function: `ALL_TENANTS`/`PROPERTY` always visible; `FLOOR`/`ROOM` matched against the tenant's own bed → room → floor chain via a JSON-path filter on `audienceFilter`, not just the audience enum alone. Also excludes not-yet-published and already-expired notices.
- `src/routes/complaints.ts` (staff-facing: list/detail/assign/status/comment) and tenant-self additions to `src/routes/tenant.ts` (file/view own complaints — with real ownership re-derivation, ownership checked by `reporterId`, not just org scoping — and post comments). `src/routes/notices.ts` (staff CRUD) and the matching tenant-self `GET /tenant/notices`.
- **Closed a real gap in the "server done and safe" gate itself**: the plan calls for a *named* regression test for the admin-login.js account-takeover bug specifically, which wasn't explicitly present — isolation.test.ts and Phase 1's property-scope tests covered the same ground implicitly, but not under a discoverable name. Added `tests/regression-account-takeover.test.ts`, framed exactly as the plan describes: an outsider with zero relationship to an org cannot read or write anything in it by supplying its ID, on any endpoint that takes one.
- **New test file** `tests/phase4-complaints-notices.test.ts` — 12 tests against live Supabase, including the literal Phase 4 exit criterion: a floor-targeted notice is visible only to tenants on that floor (proven with two tenants on two different floors in the same property, plus a parallel room-targeted case). All 59 tests (42 from Phases 0–3 + 12 new + 5 regression) pass together; zero orphaned test data.

### Server "done and safe" gate: COMPLETE
All conditions from plan §4 verified: missing/invalid tokens rejected everywhere; a valid org-A token can never touch org-B data via any path (isolation.test.ts); role/property-scope enforcement holds per resource (Phase 1 tests); every mutation produces an audit row (verified by direct count across every route file, all four phases); full tenancy + invoice/payment happy paths pass (Phases 2–3 tests); both named historical-bug regressions covered (`tests/regression-account-takeover.test.ts`, `tests/no-custom-otp.test.ts`) and passing; `server/` deployed and reachable on Render (`https://rentle-gdys.onrender.com`), verified live, independent of any frontend.

### Phase 5 — Pilot v1 mobile app: IN PROGRESS
Flutter, single codebase for Android + iOS (scope revised 2026-08-08 to cover both, not Android-only). This is the first UI surface of the whole project — everything up to this point has been API-only. See plan §4 Phase 5 for the exact in/out-of-scope feature list and distribution plan.

### Done so far
- `mobile/` scaffolded (Flutter project, `flutter analyze` clean). Deps: `clerk_flutter` (community-maintained, not official — see decisions log), `flutter_riverpod`, `go_router`, `dio`.
- `core/config/env.dart` — build-time config via `--dart-define` (Clerk publishable key, API base URL), never hardcoded.
- `core/api/api_client.dart` — Dio wrapper, token-injection interceptor pulling the Clerk session JWT per-request via a getter function (decouples the client from the auth SDK's shape).
- **First on-screen run (2026-08-08), both platforms:** launched on iOS Simulator (iPhone 17 Pro, iOS 26.5) and a freshly-created Android emulator (Pixel 7 profile, Android 15/API 35, arm64) via `flutter run --dart-define=CLERK_PUBLISHABLE_KEY=...`. Identical Clerk sign-in screen renders correctly on both — title, email field, Google OAuth, "Secured by Clerk" + "Development mode" badge — from the one shared codebase. No Android AVD existed yet on this machine; created one after fixing a `avdmanager`/Java-on-PATH issue (its bundled script wasn't finding Android Studio's JDK — fixed by exporting `JAVA_HOME` to Android Studio's bundled JRE before invoking `avdmanager`/`sdkmanager`) and downloading the `android-35;google_apis;arm64-v8a` system image.
- **First real sign-up (2026-08-08) surfaced a live bug:** after completing email OTP, the app got a 401 from `GET /me`. Root cause traced via Render's live logs: the Clerk `user.created` webhook delivery to `/webhooks/clerk` completed with **400** (svix signature verification failed), not 401 — meaning `CLERK_WEBHOOK_SECRET` set on Render no longer matched the signing secret on Clerk's current webhook endpoint, so `upsertUser` never ran and no local `User` row was ever created. Confirmed by querying the `users` table directly (empty). Unblocked immediately as a one-off by hand-upserting the local row via `clerkClient.users.getUser()`. **Fixed properly the same day** — user refreshed `CLERK_WEBHOOK_SECRET` on Render to match Clerk's current signing secret; webhook delivery confirmed working, real sign-ups now provision `User` rows automatically with no manual step.
- **Owner + tenant screens built (2026-08-08):** the rest of Pilot v1's UI, replacing the original single proof-of-concept `HomeScreen`. Architecture:
  - `core/app_router.dart` (go_router) + `main.dart` — one `ApiClient` and one `GoRouter` created per signed-in session, exposed to the widget tree via a `ProviderScope` override (`apiClientProvider`) scoped to that session, so downstream screens never touch `ClerkAuthState` directly.
  - `features/identity/identity_gate.dart` — the actual answer to "how do we know if it's an owner, manager, or tenant logging in": calls `GET /me` first. Non-empty `memberships` → staff, routed straight in (or an org picker if they belong to more than one org) using `membership.role` for UI-only permission gating (`core/permissions.dart` mirrors the server's `ROLE_PERMISSIONS` table — server is still the real enforcement). Empty `memberships` → checked against a locally-remembered tenant org (`core/tenant_prefs.dart`, `shared_preferences`) before falling back to a "no access yet" screen.
  - **A real gap found while wiring this up, not just the routing question:** a tenant's `Tenancy` only leaves `PENDING_ONBOARDING` when *their own* Clerk session calls the onboarding-accept endpoint — the owner's token can't do it for them, and complaint-filing is a tenant-only endpoint too. The plan had deferred all tenant-facing UI to post-pilot, but with zero client able to call these, no real tenant could ever be onboarded during the pilot. Decided (user, 2026-08-08) to add a minimal tenant flow now rather than post-pilot — see below.
  - Owner side (`features/owner/`): `properties/` (list, guided create form, per-property tabbed workspace), `rooms_beds/` (Buildings → Floors → Rooms drill-down, each room expandable to its beds with inline status changes — `OCCUPIED` deliberately not offered manually, matching the server's bed-status machine), `tenants/` (list with status filter, invite flow, lifecycle actions gated by the real allowed-transition table), `invoices/` (list, generate-for-month, detail with line items + payment history, record-payment), `complaints/` (list, detail with assign/status-transition/comments — no staff-side "create", matching the API).
  - **Tenant invite hand-off has no hosted web page** (would cost money to run reliably) — `core/invite_code.dart` bundles `{orgId, token}` into one opaque base64 blob the owner copies and shares out-of-band (WhatsApp/SMS); the tenant pastes it into `features/tenant/tenant_accept_screen.dart`. Tenant side also gets `tenant_home_screen.dart` (their own tenancy) and `file_complaint_screen.dart`/`tenant_complaint_detail_screen.dart` (the only client that can actually exercise the tenant-complaint endpoints end-to-end).

### Gaps closed + real-device readiness (2026-08-09)
- **Edit/delete UI added** for properties, buildings, floors, rooms, and beds — every `*_form_sheet.dart`/`property_form_screen.dart` now takes an optional `existing` object and PATCHes instead of POSTs when present; list rows got a trailing menu (Edit/Delete) alongside the existing tap-to-drill-down. Delete confirmations share one `core/widgets/confirm_delete.dart` dialog. Server-side 409s (occupied bed/room) surface as a plain error snackbar rather than a crash.
- **Document upload built** — `core/document_upload.dart` + `core/widgets/documents_section.dart`, wired into both the staff-side tenant detail screen and the tenant's own home screen. Picks a photo (camera or gallery, via `image_picker`), requests a Supabase signed upload URL from our server, PUTs the bytes straight to Supabase, then confirms so the server writes the `TenantDocument` row. **Verified the actual Supabase protocol before implementing** rather than guessing (checked `supabase/storage-js` and `supabase/storage`'s own source on GitHub): the signed-upload PUT is authorized solely by the `token` already embedded in the returned `signedUrl` — the route has no apikey/JWT preHandler at all — so the client needs zero Supabase credentials, consistent with "clients never touch Supabase directly, only server does."
- **Real bug found and fixed: Android release builds had no network access.** `android/app/src/main/AndroidManifest.xml` (the manifest that applies to every build variant) never declared `INTERNET` — only the `debug`/`profile` manifests did, which Flutter adds automatically for its own dev tooling (hot reload, debugging), not for the app's actual use. A release APK — which is what sideloading to a pilot phone actually means — would have installed and opened fine, then failed every single network call (Clerk auth included) with no obvious cause. Added the permission to the main manifest and confirmed it's present in a built release APK via `aapt dump permissions`.
- **Confirmed real-device build readiness for both platforms this session:** `flutter build apk --debug` and `--release` both succeed (release APK 61.1MB, debug-signed per `build.gradle.kts` — fine for sideloading, not Play-Store-ready); `flutter build ios --no-codesign` succeeds (Info.plist validated with `plutil -lint`, `NSPhotoLibraryUsageDescription`/`NSCameraUsageDescription` added for the new document-upload feature — missing these would have been a hard crash on first photo/camera access on a real device, not just a Simulator quirk). Bundle/application IDs already match (`com.rentle.mobile` on both platforms). Actual on-device run is still pending — needs the Clerk publishable key, never persisted anywhere — `mobile/README.md` now has exact step-by-step instructions for both an Android USB-debug run + sideloaded release APK, and an iOS real-device run via Xcode signing (free personal Apple ID, no paid account needed for personal device testing).
  - `flutter analyze`: 0 issues. `flutter test`: smoke test passing.

### Real-device testing session (2026-08-09, same day continued) — role picker built, blocked on a live webhook bug

**Real device installs succeeded.** User connected their iPhone via USB; Claude built a signed release IPA directly (`flutter build ios --release --dart-define=...`, auto-signed using the already-configured `DEVELOPMENT_TEAM = 9U2JK937S2` in the Xcode project — no manual Xcode step needed) and installed it with `flutter install -d <device-id>`, confirmed via `xcrun devicectl device info apps`. No Android device connected yet this session.

**User hit a real UX gap immediately: signing in as a fresh user went straight to "enter an invite code," with no path for an actual PG owner to register their own PG.** Fixed by building a proper role-choice screen and a real owner self-serve registration flow:
- `features/identity/no_access_screen.dart` rewritten — "I'm a PG owner → register my PG" / "I'm a tenant → I have an invite code" / a note that invited staff get access automatically + a manual "Refresh" link for them.
- `features/identity/register_pg_screen.dart` — new. Calls `ClerkAuthState.createOrganization(name: ...)` directly (confirmed via reading `clerk_auth` package source that `ClerkAuthState extends clerk.Auth`, so this method is available with zero new server endpoint — Clerk's own webhook already provisions the local `Organization`/`OrganizationMember(OWNER)` rows for any org creation, client or backend-API-triggered, identically). Exposed a `clerkAuthStateProvider` (same override pattern as `apiClientProvider`, wired in `main.dart`'s `_SignedInRootState`) so screens can reach it without touching Clerk plumbing directly.
- Polls `GET /me` for up to 3 minutes after calling `createOrganization`, since the local rows only exist once Clerk's webhook lands — this was built anticipating cold-start delay, but see below: what's actually happening turned out to be worse than delay.

**User explicitly asked about tenant "browse nearby PGs" discovery — deliberately not built.** This is a real, undesigned booking/CRM feature (cross-org property visibility, geolocation, join-request/approval flow) that the original plan explicitly deferred to the Growth tier after real competitor research. Recommended a much smaller alternative instead (typed search by PG name + owner-approval join request, no public map/browse) and asked the user to confirm scope before building anything — **still awaiting their answer**, nothing built either way yet.

**3 demo accounts created for role testing** (`owner-demo`/`manager-demo`/`tenant-demo`, "Demo PG" org pre-seeded with a property/room/2 beds, tenant-demo pre-activated on bed A, manager-demo's role hand-set to MANAGER) via a one-off script (`server/scripts/seed-demo-accounts.ts`, deleted after running — not part of the app). Two real things learned building it, both fixed:
1. Password auth alone doesn't work for fresh accounts on a new device — Clerk's device/step-up verification kicks in regardless of first factor and tries to email a code, which is a dead end for a fake `@rentledemo.com` address nobody can read. **Fixed** by adding a real, verified, primary email to each of the 3 existing Clerk users via `clerkClient.emailAddresses.createEmailAddress({verified:true, primary:true})` (keeps the same Clerk user id, so no local DB changes needed) — Gmail `+` aliases under the user's own inbox (`amaansaify64+owner@gmail.com` etc.), so any code Clerk sends is actually receivable. Current credentials: all three at password `RentleDemo123!`, but OTP via the alias email is the recommended path.
2. **While seeding, the Clerk webhook took over 90 seconds (well past a normal cold start) to provision the demo org locally** — worked around at the time by upserting the local rows directly in the seed script instead of waiting.

**That "worked around" webhook slowness turned out to be a live, unresolved bug, not just cold-start latency — this is where the day stopped.** When the user actually tried the new "register your PG" flow for real (signed in as `amaansaify64+owner@gmail.com`, tapped register), it hung on "still setting up." Investigated live:
- Server confirmed fully awake and fast: user's own Render logs show `GET /health` at 0.33ms and `GET /me` responding quickly, no cold-start delay in play.
- Directly queried the database (`prisma.organization.findMany`, `prisma.user.findMany`) — **zero rows for the new org or the `amaansaify64+owner@gmail.com` user**, even minutes after the attempt, with the server demonstrably warm the whole time. This rules out "just slow," and rules out a missing `CLERK_WEBHOOK_SECRET` too (the webhook route returns 400 "missing svix headers" on a manual unsigned test, not 503 "not configured" — the secret is set on Render, just possibly stale/wrong, or delivery isn't reaching us at all).
- Tried to inspect Clerk Dashboard → Webhooks → endpoint → Logs/Activity for actual delivery attempts and error codes — **could not get through the UI via browser automation**: the endpoint list, detail view, and logs are all rendered inside a cross-origin Svix iframe that neither click automation nor network-request reading could penetrate (`iframe.src` itself comes back `[BLOCKED: Cookie/query string data]`, and `read_network_requests` never captured any Svix-origin calls even after a fresh page load). Clerk's own (non-iframe) **Application Logs** page, by contrast, works fine via automation and confirmed all the right events fired Clerk-side (`user.created` ×1, `organization.created`, `organization_membership.created` — not shown for this exact attempt yet, but the same pattern held for the earlier demo-seeding attempt).
- **Left as a direct ask to the user** (not yet answered before the session ended): open Clerk Dashboard → Configure → Webhooks → the `rentle-gdys.onrender.com` endpoint → Logs/Activity tab themselves, and report what the actual delivery attempts show (status codes, error text), plus confirm the endpoint URL literally ends in `/webhooks/clerk` and not just the bare domain.

**Net effect: the owner-registration flow (and by extension any brand-new org/webhook-dependent flow) is currently non-functional end-to-end on a real device, blocked on this webhook delivery investigation.** Existing orgs/memberships (the demo accounts, seeded directly) still work fine for testing the Manager/Tenant paths — this only blocks *new* org creation via the app (and would equally block a *new* real Manager's invite-accept, or a *new* real tenant's first sign-up, since those also depend on the same webhook path). This is a more serious version of the same class of bug fixed on 2026-08-08 (stale `CLERK_WEBHOOK_SECRET`) and needs the same kind of direct fix, once the Clerk-side logs reveal what's actually failing.

**Session ended here at the user's request** — nothing further attempted; resume with the Clerk webhook Logs/Activity findings first thing next session. Nothing has been committed or pushed this entire round (edit/delete UI, document upload, Android/iOS real-device fixes, owner-registration flow, role-choice screen) — nothing new decided about that, still waiting to be asked.

### Webhook investigation continued (2026-08-10) — server-side conclusively ruled out

Rather than wait on the Clerk dashboard iframe, tested the deployed endpoint directly with hand-signed synthetic svix requests (throwaway scripts in `server/scripts/`, deleted after running — same one-off convention as the demo-seed script):
1. **Signature verification against Render's live secret: passes.** A no-op event type, signed with the local `server/.env` copy of `CLERK_WEBHOOK_SECRET`, got `200 {"received":true}` from `https://rentle-gdys.onrender.com/webhooks/clerk` — the deployed secret is *not* stale relative to local `.env`, ruling out the most likely repeat of the 2026-08-08 bug class.
2. **Full handler chain against realistic payloads: works end-to-end.** Hand-signed `user.created` → `organization.created` → `organizationMembership.created` events (fake throwaway Clerk IDs) all returned `200`, and a direct Prisma query afterward confirmed the `User`/`Organization`/`OrganizationMember(OWNER)` rows were created correctly, with the right role default. Test rows deleted immediately after confirming.

**Conclusion: the server, the secret, and the handler logic are all provably correct right now.** The bug is entirely upstream — either Clerk isn't actually dispatching the real `user.created`/`organization.created`/`organizationMembership.created` events to this endpoint (URL mismatch, endpoint disabled, wrong event types subscribed), or delivery is failing somewhere in Clerk/Svix's own dispatch before it ever reaches Render. **This narrows the remaining ask of the user considerably** — no longer "read the cryptic Svix log iframe," just: confirm in Clerk Dashboard → Configure → Webhooks that the endpoint is enabled, its URL is exactly `https://rentle-gdys.onrender.com/webhooks/clerk`, and it's subscribed to the `user.*`/`organization.*`/`organizationMembership.*` events — that non-iframe config view should be enough on its own to spot a misconfiguration without needing the Logs tab at all.

### Webhook bug fully resolved (2026-08-10) — two separate root causes found and fixed same day

Confirmed live: created a real Clerk user + org via the Backend API (not our own webhook, not a hand-signed synthetic request), watched our DB for the resulting rows.

1. **Wrong endpoint URL.** The Clerk webhook endpoint was configured as bare `https://rentle-gdys.onrender.com` — missing the `/webhooks/clerk` path entirely. Every real delivery 404'd (`{"message":"Route POST:/ not found"}`, confirmed directly from Clerk's Message Attempts detail view — Delivery Stats had shown 29 FAIL / 0 succeeded in the prior 24h). Browser automation could finally get into this specific config view (unlike the Logs/Activity tab, which stayed blocked behind the Svix iframe as before) once navigated to directly rather than via the list-row click, which is itself iframe-gated. User fixed the URL. This alone fixed `user.created` and `organization.created` (confirmed via a live test), but not `organizationMembership.created`.
2. **Real bug in our own handler: silent data loss on a webhook-ordering race.** `upsertMembership`/`deactivateMembership` in `src/routes/webhooks/clerk.ts` did `if (!org || !user) return;` when the referenced Organization/User row didn't exist yet — intended as "safe to skip, Svix will retry" per the existing comment, but returning normally makes the route reply `200`, which tells Svix the event was delivered successfully and **stops all retries**. Clerk fires `organization.created` and the owner's `organizationMembership.created` near-simultaneously for a brand-new org, and Svix doesn't guarantee delivery order across event types — the membership event could genuinely arrive first, and got silently, permanently dropped. Fixed by throwing instead of returning, which surfaces as a 500 and does trigger a Svix retry. **Pre-existing since Phase 0**, not something introduced recently — it just happened to be the second blocker once the URL was fixed.

Final live test after both fixes deployed: `user.created` landed in 7s, `organization.created` in 0s, `organizationMembership.created` in 10s. Owner-registration flow (and any brand-new manager/tenant signup) confirmed working end-to-end again. Fix pushed as its own isolated commit (`ab73680`) ahead of the tenant-discovery/mobile commits below, specifically so it could deploy and be verified independently.

### Tenant discovery built (2026-08-10) — search-by-name + join request, the small alternative

User confirmed: build the small alternative now (search PG by name + owner-approval join request), not full "browse nearby PGs". Built end-to-end, server + mobile, tested against the live DB.

**Server:**
- Two new models. `PropertyListing` — deliberately **not** RLS-protected, the third table (after `organizations`/`organization_members`) to carry that exception: a narrow public projection (name/city/state only, never address/phone/amenities/photos) so a prospective tenant with zero org relationship can search across every org. Kept in sync from `properties.ts`'s create/update/delete handlers via `services/property-listing.ts`, gated by a new `Property.discoverable` boolean (default `true`, owner can opt out per property). `JoinRequest` — stays fully RLS-protected like every other tenant-data table; the "tenant has no org context yet" chicken-and-egg problem (same shape as the organizations exception) is solved differently here: the create/list-mine endpoints validate the org/property against the public directory first, then open a `withOrgContext(orgId, ...)` transaction scoped to that now-verified id, so the RLS session var is legitimately set rather than bypassed.
- New route file `src/routes/join-requests.ts`: `GET /property-listings/search` (cross-org, authenticated only), `POST /organizations/:orgId/join-requests` (tenant creates, mirrors `tenant.ts`'s onboarding/accept pattern of `authenticate` + inline org validation, no membership required), `GET .../join-requests/mine` (tenant polls, single org only — never cross-org), `PATCH .../cancel`, and owner-side `GET .../join-requests`, `PATCH .../approve`, `PATCH .../reject` (all `tenancy:read`/`tenancy:write`, same permissions as the existing tenancy routes). **Approve collapses the cold-invite flow's two round trips into one** — since the tenant's identity is already known (unlike a cold invite where anyone could redeem the token), approve reserves the bed and creates the `Tenancy` directly in `PENDING_ONBOARDING`, no signed onboarding token needed. Reuses the exact same VACANT-bed check as `/tenancies/invite` so the two paths can't diverge in bed-status handling.
- Migration applied against the live Supabase DB hit a real infra gotcha worth remembering: **this network has no outbound IPv6 route, and Supabase's direct-connection host (`db.<ref>.supabase.co`) only has an AAAA record** — `prisma migrate dev`/`deploy` couldn't reach it at all (`P1001`). Worked around by pointing `DIRECT_URL` at the pooler host's **session-mode** port (5432, no `pgbouncer` param — same host `DATABASE_URL` already uses in transaction mode on 6543), which is IPv4-reachable. Separately, `app_user` (deliberately `NOSUPERUSER`/no `CREATEDB`) can't create the shadow database `migrate dev` wants — worked around with `prisma migrate diff --script` (no shadow DB needed) to generate the SQL by hand, written into a normal timestamped migration folder, applied via `prisma migrate deploy`. Both workarounds were one-off shell scripts, deleted after use — if this project ever needs another schema change from this same network, the same two-part workaround (session-pooler port for `DIRECT_URL`, `migrate diff --script` instead of `migrate dev`) will be needed again.
- Backfilled `PropertyListing` for the one pre-existing property (Demo PG) so search works on already-seeded data, not just newly-created properties.
- New test file `tests/tenant-discovery.test.ts`, 12 tests against the live DB: cross-org search, duplicate-request rejection, non-member blocked from an org's request list, full approve → `Tenancy` in `PENDING_ONBOARDING` + bed `RESERVED` → tenant sees it via `/tenant/me` with no separate accept step, reject with a note. **Found and fixed a real regression while running the full suite**: several existing test files' `afterAll` hard-`deleteMany`s a `Property` row directly — now blocked by `PropertyListing`'s FK unless the listing is deleted first (production only ever soft-deletes properties, so this never bites the app itself, only test teardown). Fixed in `phase1`/`phase2`/`phase3`/`phase4`/`regression-account-takeover`/`isolation`.test.ts and `sweep-stale-test-data.ts`. Full suite: 71/71 passing across all 9 files after the fix.

**Mobile**, all `flutter analyze` clean, `flutter test` passing, `flutter build apk --debug` verified:
- `discover_pg_screen.dart` (tenant side) — debounced search-by-name/city, tap a result → bottom sheet with an optional message → submits the join request → routes to a new status screen.
- `join_request_status_screen.dart` — polls `.../join-requests/mine` every 8s while `PENDING` (no fixed deadline, unlike `register_pg_screen`'s webhook-lag polling, since this genuinely waits on the owner's own timeline); on `APPROVED` auto-navigates straight into `tenant_home_screen` (tenancy already exists, no accept step); on `REJECTED` shows the owner's note if any with a "search again" path; tenant can cancel a still-pending request.
- `tenant_prefs.dart` gained a second, separate pref key (`pending_join_organization_id`) alongside the existing confirmed-tenancy org id, so `identity_gate.dart` can route a returning user with an unresolved request straight back to its status screen instead of falling through to "no access".
- `no_access_screen.dart` gained a third option, "I'm a tenant — find my PG", alongside the existing owner-registration and invite-code paths.
- Owner side: `join_requests_screen.dart`, a new 5th tab ("Requests") on `property_workspace_screen.dart`, listing PENDING requests for that property (filtered client-side from the org-wide endpoint) with inline reject-with-note and an approve bottom sheet that reuses the exact same vacant-bed-picker + rent/deposit form pattern as `tenant_invite_screen.dart`.

Also loaded the project's local `SKILLS/` directory (Emil Kowalski-derived design/animation philosophy skills) per the user's request, to inform these and future mobile UI decisions — applied here mainly as restraint: no new animation vocabulary introduced, matched the existing screens' plain-Material, unanimated list/form style deliberately rather than forking a new visual language for just this feature.

## Decisions log

- 2026-08-07 — Full architecture locked: Postgres/Prisma/Supabase, Clerk auth, standalone Fastify server (not Next.js), Flutter mobile-first, multi-org from day one, "core property ops only" scope, 3-tier subscription structure (Starter/Growth/Business) benchmarked against RentOk/EazyPG. See the approved plan for full reasoning.
- 2026-08-07 — Phase 0 scaffold built locally (schema, auth pipeline, RLS SQL, tests, CI), pushed to GitHub.
- 2026-08-07 — Supabase fully wired: `app_user` role, migration applied, RLS applied and proven against live data. `organizations`/`organization_members` scoped out of RLS deliberately (see gotcha #7 above) — this is a considered design decision, not a shortcut.
- 2026-08-07 — Clerk project created (Organizations enabled, email OTP + Google sign-in, phone number deliberately left off Clerk's sign-in options since Clerk now forces paid SMS verification the moment phone is enabled — collected as a plain unverified profile field in our own app instead). `server/` deployed to Render (`https://rentle-gdys.onrender.com`), Clerk webhook configured, all GitHub Actions secrets set. **Phase 0 complete.**
- 2026-08-08 — Timeline decision: the 3-PG free pilot ships on a **narrowed "Pilot v1" mobile scope** (core owner loop only, utilitarian UI, Android sideload, no app stores) rather than the full-scope Flutter app, to hit ~3–6 weeks pilot-ready instead of ~6–10+. Full mobile scope resumes post-pilot. See plan §4 Phase 5 for exact in/out-of-scope list.
- 2026-08-08 — **Phase 1 complete.** Property hierarchy + staff/RBAC API built and tested end-to-end against live Supabase, including the bed-status-machine and full audit-log coverage.
- 2026-08-08 — **Phase 2 complete.** Tenancy lifecycle built and tested end-to-end: signed onboarding tokens (no schema change needed), a tenant-context resolver parallel to resolveOrg, and Bed.status now genuinely tracks tenancy state. Also created the Storage bucket programmatically and closed a gap where test files were never actually type-checked.
- 2026-08-08 — **Phase 3 complete.** Invoicing (due-date/grace-period math, domain-level dedup per tenancy+period) and manual payments built and tested end-to-end, idempotency_keys table added and RLS-protected, daily cron now actually generates invoices instead of being an inert placeholder.
- 2026-08-08 — Scope revision: Phase 5 targets **both Android and iOS**, not Android-only as originally scoped for the pilot. No second codebase (Flutter is shared), so this is added QA surface, not doubled build effort — Android sideloading stays fully free, iOS develops/tests against the Simulator for free, and the one real new cost is the Apple Developer Program ($99/yr), deferred until an actual iPhone-owning pilot user or TestFlight distribution needs it. See plan §4 Phase 5.
- 2026-08-08 — **Phase 4 complete — all backend phases done.** Complaints (status machine + ownership-checked tenant access) and notices (real audience-targeting logic, floor/room-scoped) built and tested. Server "done and safe" gate fully passed, including a newly-added named regression test for the admin-login.js account-takeover bug that wasn't explicitly present before. Next: Phase 5, the first UI surface of the project.
- 2026-08-08 — `CLERK_WEBHOOK_SECRET` on Render refreshed by the user; real sign-ups now provision `User` rows automatically. The pilot's one remaining hard blocker from the prior session is resolved.
- 2026-08-08 — **Scope amendment to Phase 5:** added a minimal tenant-facing flow (accept-invite + file/view complaints) that the original plan had deferred to post-pilot, after discovering the tenant-onboarding-accept and complaint-filing endpoints can only ever be called by the tenant's own authenticated session — with zero client able to reach them, real tenants could never move past "invited" during the pilot itself. Kept intentionally small (2 new screens reusing the existing Clerk sign-in, no visual design pass, no notices/staff-management) so it doesn't reopen the rest of the deferred scope. Owner-side property/room/bed, tenant, invoice/payment, and complaint-management screens built out the same session — Pilot v1's full owner loop now has UI, pending a live simulator run to verify end-to-end (needs the Clerk publishable key, which isn't stored anywhere per the no-hardcoding decision).
- 2026-08-09 — Closed the two known gaps from the prior session (edit/delete UI, document upload) and did a real-device readiness pass ahead of the user testing on their own Android + iPhone: found and fixed a release-build-breaking missing `INTERNET` permission on Android, added iOS photo/camera usage descriptions, verified both `flutter build apk` (debug+release) and `flutter build ios --no-codesign` succeed, and wrote exact run/build/sideload instructions into `mobile/README.md`. Nothing committed yet — waiting on the user's own device test before that decision.
- 2026-08-09 — **Built a real owner self-serve PG registration flow + role-choice screen**, after the user hit the "no access → invite code only" dead end live on their iPhone. Uses Clerk's client-side `createOrganization` directly (zero new server endpoint, reuses the existing webhook). Deliberately did NOT build tenant "browse nearby PGs" — flagged it as an undesigned, explicitly-deferred booking/discovery feature and proposed a smaller alternative (search-by-name + join-request); awaiting the user's decision.
- 2026-08-09 — **Live blocking bug found, unresolved at end of session: the Clerk→server webhook is not reliably provisioning new orgs/users**, confirmed via direct DB queries (zero rows) against a server confirmed warm and fast — not the cold-start latency the registration flow was built to tolerate. Root cause not yet identified — browser automation couldn't get past Clerk's iframe-embedded webhook Logs UI to see actual delivery attempts/errors. **First task next session: get the user's read of Clerk Dashboard → Webhooks → endpoint → Logs, then fix accordingly** (this blocks new-org registration, and would equally block any brand-new real manager/tenant signup, not just the demo flow). Full diagnostic trail preserved above for continuity.
- 2026-08-10 — **Webhook bug narrowed, server-side ruled out**: hand-signed synthetic svix requests sent directly to the deployed endpoint prove the secret is current and the full handler chain (user/org/membership creation) works correctly end-to-end. The remaining ask of the user is now much narrower — confirm the Clerk endpoint's own config (enabled, exact URL, subscribed event types) rather than parse the Logs iframe. Still unresolved/unanswered as of this entry.
- 2026-08-10 — **Tenant discovery built**: user picked the small alternative (search-by-name + owner-approval join request) flagged 2026-08-09. New `PropertyListing` (non-RLS, public, name/city/state only) and `JoinRequest` (fully RLS) models, new `/property-listings/search` + `/organizations/:orgId/join-requests*` routes, approve collapses the cold-invite flow's two round trips into one since the tenant's identity is already known. Full mobile UI both sides (tenant search/request/status-poll screens, owner review tab on the property workspace). 12 new server tests + a real regression fix in 6 existing test files' teardown (hard-deleting a Property now needs its PropertyListing deleted first). Also hit and worked around a real infra gotcha applying the migration: this network has no IPv6 route and Supabase's direct-connection host is IPv6-only — used the pooler's session-mode port instead, and `migrate diff --script` instead of `migrate dev` since `app_user` can't create a shadow database. Full detail above.
- 2026-08-10 — **Webhook bug fully resolved.** Two root causes, found and fixed the same day: (1) the Clerk endpoint URL was missing `/webhooks/clerk` entirely — every real delivery 404'd — user fixed it in Clerk's dashboard; (2) a real, pre-existing-since-Phase-0 bug in our own handler where `upsertMembership`/`deactivateMembership` returned normally (→ 200 → no Svix retry) instead of throwing when a webhook-ordering race meant the referenced org/user row didn't exist yet, silently and permanently dropping `organizationMembership.created` for every brand-new org. Fixed to throw instead. Confirmed live end-to-end with a real Clerk-originated user+org (not synthetic): all three events (`user.created`, `organization.created`, `organizationMembership.created`) now land within seconds. Pushed as 3 scoped commits (`ab73680` webhook fix, `11aa9cc` server tenant-discovery, `231b6db` mobile) — Render auto-deployed from the push.

### Mobile UI revamp + a real navigation bug + staff/notices (2026-08-10/11)

**Visual design pass**, using the user's local `SKILLS/` design-craft skills (a `colourtheme-selector` palette skill was added mid-session too): `SignedInHeader` (email + logout, on the role-choice screen per the user's ask), a real `ThemeData` (`core/theme/app_theme.dart`) built from the "Blue Vivid + Cool Grey" palette — extends the existing brand blue rather than replacing it, one file now drives every screen's buttons/cards/chips/inputs. All 5 status-color helper functions (tenancy/complaint/priority/invoice/bed) migrated from scattered raw `Colors.green/orange/red` to theme-driven semantic colors (`AppSemanticColors` extension for success/warning, `colorScheme.error` for errors) — 12 files touched. Kept the platform-native system font deliberately rather than adding a custom one. 4 targeted animations (loading→content crossfade in the shared `AsyncValueView`, a success-checkmark entrance, the property-workspace FAB's tab-swap transition, the join-request status screen's pending→resolved crossfade) — capped well under the "5-7 max" restraint guideline the animation skills themselves call for.

**Real bug found on the user's actual device, not caught by any earlier testing**: right after the webhook fix, the owner-registration flow (name entered → org created → poll finds membership → navigate to properties) crashed with `Bad state: apiClientProvider was read before the signed-in ProviderScope override was applied` — a Riverpod `StateError` meaning the new page landed outside the nested `ProviderScope` override `main.dart`'s `_SignedInRootState` set up.

First fix attempt was wrong (or at least incomplete): isolated the trigger to `RegisterPgScreen` being the only screen opened via a raw `Navigator.push` that then called go_router's `context.go(...)` from inside itself, and patched it (plus `DiscoverPgScreen`/`TenantAcceptScreen` defensively) to route back through `/` instead of computing its own destination. This looked like it worked in that one retest, but the same crash then showed up on the **Staff screen and the main properties dashboard on a completely normal sign-in** — proving the real cause was never about which navigation path was used.

**Actual root cause**: `main.dart` nested the entire `Router` + `ProviderScope` override *inside* `ClerkAuthBuilder`'s conditional signedIn/signedOut widget swap. `ClerkAuthBuilder` decides which subtree to render by checking `auth.client.user`, and that can transiently change during any Clerk-internal background operation routed through its own `safelyCall` wrapper (session-token refresh, `createOrganization`, anything) — even for a single frame. With the Router nested inside that swap, any such flicker tore down and rebuilt the *entire app shell*, discarding the ProviderScope override and crashing whatever page happened to be open. That's why it moved around unpredictably instead of staying tied to one screen.

**Real fix**: restructured so `ClerkAuthBuilder` only ever renders the content of a single route (`AuthGate`, `core/app_router.dart`'s `/`) instead of owning the whole app. `main.dart` now uses `MaterialApp.router` with a `GoRouter` created exactly once at module load — permanently stable, never rebuilt by anything Clerk does. The signed-in `ApiClient` moved from a nested-`ProviderScope`-override pattern to a `StateProvider` (`clerkAuthStateHolderProvider`, `api_providers.dart`) set once at sign-in — there's now only ever one `ProviderContainer` for the app's whole lifetime. Confirmed fixed after rebuild: registration, staff screen, and normal sign-in all tested working. The three earlier defensive route-through-`/` patches were left in place (harmless, still technically correct) rather than reverted under time pressure. Also added a permanent debugging aid while chasing this: `AsyncValueView`'s error state now has an expandable "Details" section showing the raw exception + stack trace — valuable for any future unexpected error caught this way, not just this one, especially since live USB debugger attach to the device proved unreliable mid-session.

**Staff management and notices built** — the two remaining explicitly-deferred items from the original Phase 5 plan (`docs/../plans/hey-hey-how-are-fuzzy-bee.md` §4: "staff-management UI... notices... deferred to post-pilot"), both already fully supported server-side since Phase 1/4 with zero mobile UI until now. Staff: `StaffListScreen` (reachable via a new icon on `PropertiesListScreen`'s app bar, org-level since staff aren't property-scoped), `StaffInviteScreen` (email + role + per-property access), `StaffDetailScreen` (edit role/property-scope, deactivate, staff profile salary/join-date) — new `OrgMember`/`StaffProfile` models alongside the existing narrower `OrgMemberSummary` (which stays as-is for the complaint-assignee dropdown, different shape for a different purpose). Notices: a 6th tab on the property workspace (list/create/edit/delete, audience picker with a building→floor→room cascade for FLOOR/ROOM targeting — known rough edge: editing an existing FLOOR/ROOM notice can't pre-fill the building/floor pickers since `Notice` only carries the resolved floorId/roomId, not its ancestor building), plus a read-only notices section on the tenant home screen.

### Perceived slowness + a real duplicate-data bug (2026-08-11, same day continued)

User reported the app "feels slow" — 20-second loads — and, concretely, adding a property once resulted in two properties. Both traced to the same root cause: **Render's free tier spins down after 15 minutes idle, and the only keep-alive ping (`daily-jobs.yml`) ran once a day** — nowhere near enough to prevent repeated cold starts during normal use with gaps between actions. Fixed with a new `.github/workflows/keep-alive.yml`, pinging the public `/health` endpoint every 10 minutes (needs a push to actually start running — doesn't help until then, and whatever request is in flight when this is written will still be slow one more time).

That explains the duplicate directly: a 30-60s cold-start save looks stuck, the user backs out of the form (which doesn't cancel the in-flight request — the property gets created anyway), then retries, creating a real second row. Fixed narrowly but concretely: `PopScope(canPop: !_saving, ...)` added to every full-screen create/edit form (property, tenant invite, staff invite, staff detail, file-complaint, invite-accept) so navigating away mid-save is now impossible — the user just waits for the spinner either way. **Known gap, not yet fixed**: several *bottom-sheet* forms (building/room/bed, notices, payments) have the same underlying risk but `PopScope` doesn't block a sheet's swipe-to-dismiss/tap-outside-to-dismiss — needs a different mechanism (dynamically toggling `isDismissible`/`enableDrag`, or a rebuild of how those sheets are shown). Left for a future session unless it's hit in practice first.

**Status: code complete, `flutter analyze`/`flutter test` clean, verified live on device for the ProviderScope fix. Not committed yet** — user asked for a commit (not a push) at the end of this session; they'll push it themselves.
